@model EmptyCore.ViewModels.RestaurantViewModel
@{
    ViewBag.Title = "Home";
}

<h1>index</h1>
<h2>All Restaurant</h2>
@foreach (var item in Model.Restaurants)
{
    @await Html.PartialAsync("_Summary",item)
}
@*<table>
        @foreach (var item in Model.Restaurants)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>
                    <a href="/home/details/@item.Id">details</a>
                    @Html.ActionLink("Go", "details", new { id = item.Id })
                    <a asp-action="Details" asp-route-id="@item.Id">More</a>
                    <a asp-page="/Restaurants/Update" asp-route-id="@item.Id">Edit</a>
                </td>

            </tr>
        }
    </table>*@
<div><a class="btn btn-primary" asp-action="Create">Add Restaurant</a></div>
